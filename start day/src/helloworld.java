
public class helloworld {
	
	
		public static void main(String[] args) {
			double gpa = 4.0;// the 4 is a literal
			int number;
			boolean isGraduated = false;
			// single quote marks denote a char compared to double quotes for a string
			/*
			 * the expression "i have a " + gpa + "gpa"
			 * involves two literals
			 * 1. "i have a " -- a string literal
			 * 2. "gpa" -- a string literal
			 * and one variable
			 * 1. gpa -- a double variable
			 * when you use a double with a string the double gets converted to a string
			 * 
			 */
			char c = '\u0041';
			char d = 'f';
			int x, y;
			x = 5; // not an intialization
			y = 10;
			System.out.println(y + "/" + x + " = " + (y/x));
		}
	

}
